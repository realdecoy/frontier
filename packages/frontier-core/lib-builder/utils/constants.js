// if at some point this package becomes more flexible w/r/t config, these could
// become defaults instead of the only possible values
module.exports = {
  BABEL_MODULE_ENV: 'module',
  ESNEXT_TEMP_DIR: 'esnext',
  MODULE_DIR: 'module',
};
